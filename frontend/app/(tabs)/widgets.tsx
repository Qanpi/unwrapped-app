import { ThemedText } from "@/components/ThemedText";
import { SafeAreaView } from "react-native";

export default function WidgetsScreen() {
    return (
        <SafeAreaView>
            <ThemedText>Widgets</ThemedText>
        </SafeAreaView>
    )
}